# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sw?

# Env files
.env
.env.*
!.env.example

# Database and Cache
*.sqlite
*.sqlite3
*.db
__pycache__/
*.pyc

# Ferramentas auxiliares (fora do app principal)
scrapper/
bpm_seeder/bin/
plan/
scratch/
zap-scan-export/

# Graphify: versiona o knowledge graph, ignora o cache local
graphify-out/cache/

# Prompts e notas para os agentes de backend — material de trabalho, não do app
docs/

# Dumps/backups do banco de produção — nunca versionar (o .zip tem centenas de MB)
cifras_data_backup.zip
*.sqlite.zip

# Instruções locais do Claude Code — valem só nesta máquina.
# O guia de agentes que vale para todo mundo é o AGENTS.md, esse sim versionado.
CLAUDE.md
.claude/

# Material de referência (livros de harmonia em PDF). Fora do versionamento por dois
# motivos independentes: são centenas de MB, e são obras de terceiros com direitos
# reservados — este repositório é público e AGPL, e não pode carregar conteúdo alheio.
referencia/
